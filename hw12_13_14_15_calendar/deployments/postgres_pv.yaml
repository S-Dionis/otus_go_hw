apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ .Values.appName }}-postgres-pv
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  hostPath:
    path: {{ .Values.postgres.path }}
