apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.appName }}
spec:
  selector:
    app: {{ .Values.appName }}
  ports:
    - port: 8888
      targetPort: 8888
      name: http
    - port: 8080
      targetPort: 8080
      name: grpc